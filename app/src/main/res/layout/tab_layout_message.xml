<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/linearLayout"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/tab_item_icon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:padding="5dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:src="@drawable/ic_home_message" />

    <Space
        android:id="@+id/space"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginEnd="10dp"
        app:layout_constraintBottom_toBottomOf="@id/tab_item_icon"
        app:layout_constraintTop_toTopOf="@id/tab_item_icon"
        app:layout_constraintEnd_toEndOf="@id/tab_item_icon" />

    <TextView
        android:id="@+id/tab_item_number"
        android:layout_width="16dp"
        android:layout_height="16dp"
        android:layout_marginTop="3dp"
        android:layout_marginEnd="180dp"
        android:background="@drawable/badge_circle"
        android:gravity="center"
        android:textColor="#FFF"
        android:textSize="@dimen/badge_font_size"
        android:visibility="gone"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@id/space"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="99"
        tools:visibility="visible" />

    <com.github.ybq.android.spinkit.SpinKitView
        android:id="@+id/tutorial_dot_nav"
        style="@style/TutorialDotStyle"
        android:layout_width="@dimen/dot_size"
        android:layout_height="@dimen/dot_size"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true"
        android:layout_gravity="center"
        android:visibility="gone"
        tools:visibility="visible"
        app:SpinKit_Color="#ffbb5b"
        app:layout_constraintBottom_toBottomOf="@+id/tab_item_icon"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@+id/tab_item_icon"
        app:layout_constraintVertical_bias="1.0"
        tools:background="@color/red" />
</androidx.constraintlayout.widget.ConstraintLayout>
